[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[nixpacks]
# Указываем, что исходный код лежит в папке backend
root = "backend"

[nixpacks.python]
# Явно указываем, что это Python проект
version = "3.11"